import javax.swing.JFrame;
import javax.swing.WindowConstants;

public class mainframe extends JFrame {
    public void init() {
        setTitle("Hello this is first program");
        setSize(600, 400);
        setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        setVisible(true);
    }
}
